# Add image assets here (optional)

This minimal upload includes only the **video** (`Untitled (71).mp4`).  
Image placeholders will show until you add the PNGs from your full project’s `src/assets` (e.g. the hex-named `.png` files). Copy them into this folder to restore full visuals.
